# Homebase Environment Configuration
# Copy this file to .env.local and fill in your actual values

# ============================================================================
# REQUIRED - Phase 1 (Current)
# ============================================================================

# Database Connection
# Get your connection string from Neon Dashboard (https://console.neon.tech)
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# JWT Authentication Secret
# Generate a secure random string with: openssl rand -base64 32
# CRITICAL: Use a unique secret for production (never use fallback in jwt.ts)
# Minimum 32 bytes recommended for HS256 algorithm
JWT_SECRET="generate-with-openssl-rand-base64-32"

# Application Environment
# Options: development, production, test
# Affects error messages, logging, and performance optimizations
NODE_ENV="development"

# ============================================================================
# OPTIONAL - Phase 1 (Locale Configuration)
# ============================================================================

# Locale Settings
# Customize date/time display and currency formatting for your household
# These are optional; defaults are shown below
NEXT_PUBLIC_TIMEZONE="UTC"
NEXT_PUBLIC_CURRENCY="USD"
NEXT_PUBLIC_CURRENCY_SYMBOL="$"
NEXT_PUBLIC_LOCALE="en-US"

# ============================================================================
# OPTIONAL - Phase 1
# ============================================================================

# Locale Settings
# Customize date/time display and currency formatting for your household
# These are optional; defaults are shown below
NEXT_PUBLIC_TIMEZONE="UTC"
NEXT_PUBLIC_CURRENCY="USD"
NEXT_PUBLIC_CURRENCY_SYMBOL="$"
NEXT_PUBLIC_LOCALE="en-US"

# ============================================================================
# FUTURE - Phase 2+ (Notifications & Push)
# ============================================================================

# Web Push Notifications (Phase 2)
# Generate VAPID keys with: npx web-push generate-vapid-keys
# Public key is safe to expose to client (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""

# SMS Notifications via Twilio (Phase 2)
# Get credentials from Twilio Console (https://console.twilio.com)
# Used for escalated reminders when bills are overdue
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_PHONE_NUMBER=""

# ============================================================================
# FUTURE - Phase 3+ (Weather Integration)
# ============================================================================

# OpenWeather API
# Sign up at https://openweathermap.org/api
# Used for location-based context (e.g., notify about lawn care when weather permits)
OPENWEATHER_API_KEY=""

# ============================================================================
# FUTURE - Phase 12+ (JARVIS AI Assistant)
# ============================================================================

# Ollama Self-Hosted LLM
# Install Ollama from https://ollama.ai
# Default local endpoint; change if running on different host/port
OLLAMA_URL="http://localhost:11434"
